{{ form_start(form) }}
    {{ form_row(form.title, {'attr': {'class': 'form-control', 'placeholder': 'Pavadinimas'}}) }}
    {{ form_row(form.description, {'attr': {'class': 'form-control', 'placeholder': 'Aprašymas'}}) }}
    {{ form_row(form.deadline, {'attr': {'class': 'form-control col-4', 'placeholder': 'Atsiimti iki'}}) }}
    {{ form_row(form.pictureFile, {'attr': {'class': 'form-control', 'placeholder': 'Nuotrauka'}})}}
    {#<img src="{{ vich_uploader_asset(product, 'pictureFile')|imagine_filter('square') }}">#}
    {{ form_row(form.status, {'attr': {'class': 'custom-control custom-checkbox'}}) }}

<div class="reg-btn">
    <button class="login-btn">{{ button_label|default('Įrašyti') }}</button><a href="{{ path('product_index') }}">Atšaukti</a>
</div>
{{ form_end(form) }}


