{% extends 'base.html.twig' %}

{% block title %}Atnaujinti vietą{% endblock %}

{% block body %}
    <div class="container">
        <h1>Atnaujinti vietą</h1>
        <div id="mapEdit"></div>
        {{ include('location/_form.html.twig', {'button_label': 'Atnaujinti'}) }}
            <div class="col-md-6 form-padding-right">
                <button class="btn btn-danger"><a href="{{ path('user_show', {'id': app.user.id}) }}">Grįžti</a></button>
            </div>
        </div>
    </div>
{% endblock %}
